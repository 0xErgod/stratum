[package]
name = "stratum-lts"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Labelled transition system over ρ-calculus reduction — the trace grain."

[dependencies]
stratum-core = { path = "../stratum-core" }
