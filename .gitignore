/target
Cargo.lock
**/*.rs.bk
.playwright-mcp/

# Coq / Rocq build artifacts (repo-wide `**` match; in practice only in proofs/)
# — the .v sources are tracked, not these
**/*.vo
**/*.vok
**/*.vos
**/*.glob
**/.*.aux
**/.lia.cache
**/.nia.cache
**/.coqdeps.d

# node / tree-sitter build cruft (the generated parser.c IS tracked)
**/node_modules/


# Extraction/corpus intermediate (repo-wide `**` match; generated by proofs/Extract.v)
**/corpus_raw.out
