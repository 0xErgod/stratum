/target
Cargo.lock
**/*.rs.bk
.playwright-mcp/

# Coq / Rocq build artifacts (proofs/) — the .v sources are tracked, not these
**/*.vo
**/*.vok
**/*.vos
**/*.glob
**/.*.aux
**/.lia.cache
**/.nia.cache
**/.coqdeps.d

# node / tree-sitter build cruft (the generated parser.c IS tracked)
**/node_modules/

